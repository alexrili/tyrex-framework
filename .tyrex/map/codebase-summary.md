# Codebase Summary - Tyrex Framework

> Generated by `/tyrex-init` on 2026-03-07

## Project Metrics

| Metric          | Value                    |
|-----------------|--------------------------|
| Total files     | ~105 (mostly .md templates) |
| Source code     | 1 JavaScript file, 417 lines |
| Templates       | ~100 markdown/YAML files |
| Test coverage   | 0% (no tests)            |
| Dependencies    | 0 (zero)                 |
| Git commits     | 1 (initial commit)       |
| Git remote      | `git@github.com:alexrili/tyrex-framework.git` |
| Branch          | `main`                   |

## Source Code (bin/tyrex.js)

Single-file CLI with these key functions:

| Function                     | Line | Purpose |
|------------------------------|------|---------|
| `main()`                     | —    | Entry point — interactive setup flow |
| `installCommands()`          | —    | Copy command .md files to agent directory |
| `installTyrexStructure()`    | —    | Create `.tyrex/` and `docs/` with templates |
| `copyTemplate()`             | 102  | Template interpolation engine (`{{KEY}}` → value) |
| `handleUninstall()`          | 378  | Remove agent command directories |
| `printHelp()`                | —    | Display CLI usage |
| `ask()` / `choose()` / `confirm()` | — | Readline prompt helpers |
| `ensureDir()`                | —    | `mkdir -p` wrapper |

## CLI Interface

```
npx tyrex-framework [options]

--claude / --opencode / --cursor / --codex / --all
--local (-l) / --global (-g)
--defaults (-d) / --uninstall
--version (-v) / --help (-h)
```

## 16 Slash Commands

Defined in `templates/commands/unified/`, installed to all 4 agent directories:

| Workflow        | Shortcuts       | Management        | Documentation     |
|-----------------|-----------------|-------------------|-------------------|
| `/tyrex-init`   | `/tyrex-quick`  | `/tyrex-status`   | `/tyrex-skills`   |
| `/tyrex-new`    | `/tyrex-handoff` | `/tyrex-resume`  | `/tyrex-readme`   |
| `/tyrex-plan`   |                 | `/tyrex-settings` | `/tyrex-openapi`  |
| `/tyrex-do`     |                 | `/tyrex-evolve`   | `/tyrex-wiki`     |
| `/tyrex-review` |                 |                   | `/tyrex-help`     |

## Naming Conventions

- **Files:** lowercase, hyphenated (`tyrex-init.md`)
- **Directories:** lowercase (`commands/`, `templates/`)
- **JS Constants:** UPPER_SNAKE_CASE (`AGENTS`, `COLORS`, `VERSION`)
- **JS Functions:** camelCase (`copyTemplate`, `installCommands`)

## What's Missing (for maturity)

- [ ] README.md at project root
- [ ] Tests (any framework)
- [ ] CI/CD pipeline
- [ ] Linting (ESLint/Prettier)
- [ ] package-lock.json
- [ ] package.json scripts section
- [ ] .nvmrc / .node-version
- [ ] CONTRIBUTING.md
